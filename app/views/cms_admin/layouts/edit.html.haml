%h2 Editing Layout

- form_for :layout, :url => {:action => :update}, :html => {:method => :put} do |form|
  = render :partial => form
  .submit
    = form.submit 'Edit Layout'
    or 
    = link_to 'Cancel', cms_admin_layouts_path