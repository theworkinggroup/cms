%h2 New Layout

- form_for :layout, @layout, :url => {:action => :create } do |form|
  = render :partial => form
  .submit
    = form.submit 'Create Layout'
    or 
    = link_to 'Cancel', cms_admin_layouts_path