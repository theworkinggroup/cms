= link_to 'Add New Page', new_cms_admin_page_path, :class => 'big_button'

%h2 Pages

- if (@sites)
  - @sites.each do |site|
    .tree_structure
      = render(:partial => 'tree_branch', :object => @cms_pages[site.id])
- else
  .tree_structure
    = render(:partial => 'tree_branch', :object => @cms_pages)
