%h2 Editing Page

- form_for :cms_page, :url => {:action => :update}, :html => {:method => :put} do |form|
  = render :partial => form
  .submit
    = form.submit 'Edit Page'
    or 
    = link_to 'Cancel', cms_admin_pages_path