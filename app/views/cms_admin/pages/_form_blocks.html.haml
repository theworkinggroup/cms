- @layout ||= @page.cms_layout
- @layout && @layout.tags(:view => self, :page => @page).reject{|t| !t.class.has_form?}.each do |tag|
  
  .form_element
    .label= tag.form_label
    .field= tag.form_input