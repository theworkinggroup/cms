.form_element.select
  = form.label :cms_layout_id, 'Layout'
  = form.select :cms_layout_id, CmsLayout.options_for_select, {}, :onchange => remote_function(:url => {:controller => :layouts, :action => :form_blocks}, :with => "'id=' + value")
  
#form_blocks
